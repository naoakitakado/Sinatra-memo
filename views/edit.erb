<section>
        <h1>
          <a href="/">メモアプリ</a>
        </h1>
</section>
<section>
    <form class="edit-layout" action="/memo/<%= params[:id] %>/edit" method="POST">
        <input type="hidden" name="_method" value="PATCH">
        <input class="title-edit-memo" name="title" value='<%= CGI.escapeHTML(@memos[params[:id]]['title']) %>' required>
        <textarea class="content-edit-memo" name="content" required><%= CGI.escapeHTML(@memos[params[:id]]['content']) %></textarea>
        <input class="add-edit-memo" type="submit" value="更新">
    </form>
</section>
